# ============================================================
# Variables de entorno de WauPiura (backend)
# Copia este archivo como ".env" en esta misma carpeta (backend/)
# y ajusta los valores según tu MySQL local.
# El .env NO se sube a Git (está en .gitignore); este .example sí.
# ============================================================

DB_HOST=localhost
DB_PORT=3306
DB_NAME=WauPiura
DB_USER=root
DB_PASSWORD=diego123
